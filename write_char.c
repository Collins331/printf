#include main.h

/**
* write_char- writes the character to stdout
* @c : character expected to print
* Return: return 1 when program runs successfully
*/
int write_char(char c)
{
	return (1, &c, 1);
}
